# AI Agent Communication System - Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework for TTS server
fastapi>=0.104.0

# ASGI server for FastAPI
uvicorn[standard]>=0.24.0

# Neural TTS engine
piper-tts>=1.2.0

# Optional: For better JSON handling in scripts
# jq is used by some scripts but is a system dependency, not Python